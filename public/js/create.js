  function createCotacao(id) {
      window.location.href = "/cotacao/" + id;
  }

  function createClientes(id) {
      window.location.href = "/cliente/" + id;
  }

  function createFornecedores(id) {
      window.location.href = "/fornecedor/" + id;
  }

  function createProdutos(id) {
      window.location.href = "/produto/" + id;
  }

  function createMarcas(id) {
      window.location.href = "/marca/" + id;
  }

  function createCanais(id) {
      window.location.href = "/canais/" + id;
  }

  function createEmpresas(id) {
      window.location.href = "/empresa/" + id;
  }
